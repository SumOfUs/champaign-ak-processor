version: 1
cron:
 - name: "update-share-statistics"          # required - unique across all entries in this file
   url: "/update-share-statistics"              # required - does not need to be unique
   schedule: "*/5 * * * *"    # required - does not need to be unique
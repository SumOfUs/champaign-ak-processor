processor:
  build: .
  volumes:
    - .:/myapp
  ports:
    - "3002:3002"
  external_links:
    - champaign_db_1
  environment:
    PG_USERNAME: postgres
    PG_HOST: champaign_db_1

  command: bundle exec shoryuken -R -C config/shoryuken.yml
